import { DeleteRepeatsPipe } from './delete-repeats.pipe';

describe('DeleatRepeatsPipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteRepeatsPipe();
    expect(pipe).toBeTruthy();
  });
});
